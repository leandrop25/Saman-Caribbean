class Venta:

    def __init__(self, cliente, monto_total, descuento,total):
        self.cliente = cliente
        self.monto_total = monto_total
        self.descuento = descuento
        self.total = total
